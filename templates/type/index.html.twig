{% extends 'base.html.twig' %}

{% block title %}Type index{% endblock %}

{% block body %}
    <h1>Type index</h1>

   <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nom</th>
                <th scope="col">Description</th>
                <th scope="col">actions</th>
                

                
            </tr>
        </thead>
        <tbody>
        {% for type in types %}
            <tr class="table-info">
                <td>{{ type.id }}</td>
                <td>{{ type.name }}</td>
                <td>{{ type.description }}</td>
                
                
                <td>
                    <a class="btn btn-info btn-sm" href="{{ path('type_show', {'id': type.id}) }}">détails</a>
                    <a class="btn btn-success btn-sm" href="{{ path('type_edit', {'id': type.id}) }}">éditer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_new') }}">créer</a>
{% endblock %}
