{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
    <h1>Modifications</h1>

    {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('book_index') }}" class="btn btn-success btn-sm">retour</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
